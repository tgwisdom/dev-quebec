<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CIS | Users</title>

    <style>
      p {
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <ul>
      <% for (let user of users) { %>
      <li>
        <p><%= user.name %>:</p>
        <a href="<%= user.link %>" target="_blank"><%= user.link %></a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
